# SavePinner 产品需求文档（PRD）

> **文档版本**：v1.2（已纳入线路 A UI/UX 规范）  
> **创建日期**：2026-07-20  
> **整理日期**：2026-07-20  
> **产品状态**：待技术与合规验证  
> **目标版本**：MVP  
> **产品负责人**：待定  
> **技术负责人**：待定

---

## 1. 文档目的

本文档用于统一 SavePinner 的产品范围、用户流程、功能需求、技术边界、验收标准与上线门槛，供产品、设计、开发、测试和运营共同使用。

本文档中的市场流量、关键词难度、竞品流量、广告收入和排名周期均视为**待验证假设**，不作为开发完成或商业成功的承诺。

---

## 2. 产品概述

### 2.1 一句话描述

SavePinner 是一个无需注册的在线工具，用户粘贴受支持的 Pinterest Pin 链接后，可以预览可用媒体，并在拥有相应权利或授权的前提下下载图片、GIF 或视频文件。

### 2.2 产品定位

| 维度 | 定义 |
|---|---|
| 产品形态 | Web 工具站 |
| 首发市场 | 全球英语用户 |
| 首发平台 | 移动端 Web + 桌面端 Web |
| 核心价值 | 无需注册、操作简单、结果清晰、错误可解释 |
| 商业模式 | MVP 阶段不变现；验证后评估 AdSense |
| 技术方向 | Next.js + 服务端解析/代理下载 |
| 品牌关系 | 非 Pinterest 官方产品，不暗示合作、授权或背书 |

### 2.3 产品原则

1. **先验证数据来源，再扩大功能范围。**
2. **用户先获得结果，内容模块不得阻碍核心任务。**
3. **不承诺无法稳定兑现的“原图”“无水印”或“支持全部格式”。**
4. **不长期存储用户提交的 URL 或下载文件。**
5. **搜索增长建立在独立页面价值之上，不为关键词机械复制页面。**
6. **广告不得伪装成下载按钮或靠近核心操作区域。**
7. **线路 A 的核心体验是“粘贴 → 下载”，界面优先速度感和零摩擦。**
8. **工具、落地页内容和结果展示必须在同一页面完成，不为核心任务增加页面跳转。**

---

## 3. 背景与机会假设

### 3.1 用户问题

部分用户希望将 Pinterest 中的媒体保存到本地，用于个人备份、灵感整理或已获授权的内容工作流，但可能遇到以下问题：

- Pinterest 仅对部分图片 Pin 提供原生下载入口；不同终端的入口不一致。
- 用户难以判断当前页面展示的是缩略图、压缩版本还是较高分辨率版本。
- 视频、GIF 和短链的处理方式不一致。
- 部分第三方工具存在弹窗多、按钮混淆、结果不可解释或下载失败的问题。

### 3.2 待验证的市场假设

以下内容来自原始材料，必须在投入推广预算前重新核验：

| 假设 | 原始说法 | 验证要求 |
|---|---|---|
| 核心搜索需求存在 | `pinterest image downloader` 等词存在搜索量 | 记录 US/en 的搜索量、趋势、SERP 类型和查询日期 |
| 新站可以进入核心 SERP | 核心词 KD 较低、部分竞品域龄较短 | 审查当前前十、域名强度、内容类型、功能质量和排名稳定性 |
| 体验可以形成差异 | 部分竞品加载慢、弹窗多 | 对前三名完成移动端任务时间和成功率基准测试 |
| 广告可以覆盖成本 | 10 万月访问可能产生广告收入 | 使用有效页面浏览、地区、AdBlock 和页面 RPM 做低/中/高情景 |
| 多语言可以扩大流量 | es/pt/id 等市场存在需求 | 每个语言单独验证需求，不机械复制英文页面 |

### 3.3 当前竞争判断

本项目属于低门槛、易复制、平台依赖较强的工具方向。仅凭“更快、更漂亮、免费、无需登录”不足以构成长期壁垒。

MVP 的差异化目标限定为：

- 更清楚地展示媒体类型、尺寸和下载结果。
- 对失败原因给出可操作提示，而不是空白或通用报错。
- 核心流程无注册、无弹窗、无误导性广告。
- 对隐私、数据保留、版权与非官方关系进行透明说明。

---

## 4. 目标与非目标

### 4.1 MVP 目标

1. 用户可以粘贴一个受支持的 Pin URL 并获得解析结果。
2. 系统可以识别图片、GIF 或视频，并展示真实可用的媒体版本。
3. 用户可以通过受控下载接口保存文件。
4. 系统可以防止 SSRF、开放代理、超大文件和高频滥用。
5. 首页的主要内容可被搜索引擎直接读取，结果页和参数页不进入索引。
6. 团队可以通过监控数据判断解析成功率、性能、成本和主要失败原因。

### 4.2 MVP 非目标

- 不支持登录 Pinterest 账户。
- 不支持私密 Pin 或私密 Board。
- 不支持整板批量下载。
- 不支持自动抓取用户 Feed、搜索结果或个人主页。
- 不提供内容编辑、转码、去水印或 AI 增强。
- 不在 MVP 首发多语言版本。
- 不开发浏览器扩展、原生 App 或桌面客户端。
- 不保证任何 Pinterest URL 永久可用。
- 不保证搜索排名、收录时间或广告收入。

---

## 5. 目标用户与使用场景

### 5.1 目标用户

| 用户类型 | 主要任务 | 价值 | 优先级 |
|---|---|---|---|
| 普通用户 | 保存单个公开 Pin 的媒体文件 | 操作简单、无需安装 | P0 |
| 设计师/素材整理者 | 获取可用尺寸并整理灵感素材 | 尺寸明确、结果稳定 | P0 |
| 内容创作者 | 备份自己或已获授权的媒体 | 清晰的质量选择 | P1 |
| 运营/研究人员 | 分析公开 Pin 的媒体信息 | 元数据显示 | P2 |

### 5.2 用户故事

- 作为普通用户，我希望粘贴 Pin 链接后立即看到可下载内容，以便快速保存到本地。
- 作为移动端用户，我希望页面按钮清晰且没有误导性广告，以免误触。
- 作为素材整理者，我希望看到媒体尺寸、格式和文件大小，以便选择合适版本。
- 作为遇到失败的用户，我希望知道是链接无效、内容不可访问、格式不支持还是服务暂时异常。
- 作为隐私敏感用户，我希望知道系统是否保存我的链接和下载内容。

### 5.3 核心用户流程

```text
进入首页
  → 点击粘贴按钮或直接粘贴 Pinterest URL
  → 客户端做基础格式检查
  → 提交解析请求
  → 服务端校验并解析 URL
  → 在当前页面返回媒体信息或明确错误
  → 用户选择可用版本
  → 服务端校验短期下载令牌并流式返回文件
  → 页面提供继续解析另一个链接的入口
```

---

## 6. MVP 功能需求

### 6.1 URL 输入与校验

#### FR-001：输入 Pinterest URL

**描述**：用户可以在首页输入框粘贴一个 URL 并发起解析。

**支持范围**：

- `https://www.pinterest.com/pin/{id}/`
- Pinterest 合法国家/地区域名中的 `/pin/{id}/`
- `https://pin.it/{shortcode}` 短链

**验收标准**：

- 空输入时不发起网络请求，并显示“请粘贴 Pinterest 链接”。
- 非 HTTPS、非 Pinterest 域名或不符合路径规则的 URL 被拒绝。
- 输入框内或紧邻输入框提供清晰的粘贴按钮；在浏览器允许 Clipboard API 时一键读取剪贴板，权限被拒绝或 API 不可用时提示用户手动粘贴。
- 移动端粘贴按钮与下载按钮均保持可触达，不要求用户长按输入框才能完成核心操作。
- 提交后按钮进入加载状态，避免重复提交。
- 输入框、按钮和错误信息支持键盘及屏幕阅读器。

#### FR-002：解析短链

**描述**：服务端跟随 `pin.it` 重定向并获得最终 Pin URL。

**验收标准**：

- 最多跟随 3 次重定向。
- 每次跳转都重新执行域名和协议校验。
- 最终地址不是允许的 Pinterest Pin URL 时终止请求。
- 超时或重定向循环返回可识别错误码。

### 6.2 媒体解析

#### FR-003：解析图片 Pin

**描述**：对受支持的公开图片 Pin 返回可验证的图片版本。

**结果字段**：

- 媒体类型
- 标题（若可用）
- 预览图
- 图片格式
- 宽度与高度（若可用）
- 可下载版本列表

**验收标准**：

- 仅展示服务端实际验证成功的 URL，不根据字符串替换虚构“原图”。
- 每个版本均通过状态码、MIME 类型和大小上限检查。
- 无法确认原始分辨率时使用“最高可用分辨率”，不得标记为“Original”。
- 不支持或不可访问时返回明确错误，而不是空结果。

#### FR-004：解析 GIF

**描述**：当源内容确实为 GIF 或其他动图格式时，返回可用的动态媒体版本。

**验收标准**：

- 通过响应 MIME 和文件特征判断格式，不只依赖 URL 后缀。
- 静态预览与实际下载格式一致。
- 若只能获取静态封面，必须标记为“预览图”，不能声称为 GIF。

#### FR-005：解析视频 Pin

**描述**：对验证范围内的公开视频 Pin 返回可用视频版本。

**验收标准**：

- 展示格式、分辨率和文件大小（若可获取）。
- 按实际分辨率从高到低排序。
- 下载前验证视频 URL 的主机、MIME 和大小限制。
- 没有可用视频源时显示“当前 Pin 无可下载视频版本”。

### 6.3 结果展示与下载

#### FR-006：结果卡片

**描述**：解析成功后展示一个清晰、可访问的结果区域。

**验收标准**：

- 展示缩略预览、媒体类型和版本列表。
- 每个下载按钮清楚标注格式与尺寸。
- 解析结果在首页工具下方原位展开，不跳转到独立结果页。
- 解析期间显示 10% → 35% → 65% → 100% 的分段进度反馈；实际结果未就绪前不得显示 100%，同时提供可被辅助技术读取的加载状态。
- 页面不自动开始下载。
- 用户可以返回输入框继续处理另一个链接。

#### FR-007：受控下载

**描述**：用户点击下载后，通过服务端签发的短期令牌获取文件。

**接口原则**：

- 不接受客户端直接传入任意远程 URL 作为代理目标。
- 解析成功后由服务端生成有效期不超过 5 分钟的签名令牌。
- 下载接口根据令牌中的已验证目标执行流式转发。

**验收标准**：

- 响应包含正确的 `Content-Type` 和安全的 `Content-Disposition` 文件名。
- 过期、被篡改或重复滥用的令牌被拒绝。
- 远程文件超出大小限制时中止传输并记录原因。
- 下载失败不暴露内部堆栈、密钥或上游完整响应。

### 6.4 错误处理

#### FR-008：标准错误模型

| 错误码 | 用户提示 | HTTP 状态 |
|---|---|---:|
| `INVALID_URL` | 请输入有效的 Pinterest Pin 链接 | 400 |
| `UNSUPPORTED_URL` | 暂不支持此类 Pinterest 页面 | 400 |
| `REDIRECT_REJECTED` | 短链跳转到了不受支持的地址 | 400 |
| `PIN_NOT_PUBLIC` | 此内容不可公开访问 | 403 |
| `MEDIA_NOT_FOUND` | 未找到可下载的媒体文件 | 404 |
| `UNSUPPORTED_MEDIA` | 暂不支持此媒体类型 | 422 |
| `RATE_LIMITED` | 请求过于频繁，请稍后重试 | 429 |
| `UPSTREAM_BLOCKED` | 暂时无法访问源内容，请稍后再试 | 502 |
| `RESOLVE_TIMEOUT` | 解析超时，请稍后重试 | 504 |
| `INTERNAL_ERROR` | 服务暂时不可用 | 500 |

**验收标准**：

- 前端根据错误码显示具体提示。
- 所有失败状态均提供“重新尝试”或“检查链接”的下一步。
- 日志记录内部原因，但不记录敏感参数和完整媒体 URL。

---

## 7. 接口契约

### 7.1 解析接口

```http
POST /api/resolve
Content-Type: application/json

{
  "url": "https://www.pinterest.com/pin/123456789/"
}
```

成功响应示例：

```json
{
  "requestId": "req_abc123",
  "type": "image",
  "title": "Example Pin",
  "previewUrl": "/api/preview/preview_token",
  "variants": [
    {
      "id": "variant_1",
      "label": "736 × 1104",
      "format": "jpg",
      "width": 736,
      "height": 1104,
      "bytes": 284193,
      "downloadToken": "signed_short_lived_token"
    }
  ]
}
```

错误响应示例：

```json
{
  "requestId": "req_abc123",
  "error": {
    "code": "MEDIA_NOT_FOUND",
    "message": "未找到可下载的媒体文件"
  }
}
```

### 7.2 下载接口

```http
GET /api/download/{downloadToken}
```

约束：

- 令牌短期有效并包含目标主机、资源标识、MIME 和大小限制。
- 服务端仅允许经过解析接口验证的 Pinterest CDN 主机。
- 不提供通用 URL 转发能力。

---

## 8. 内容与页面需求

### 8.1 MVP 页面清单

| 页面 | URL | 是否索引 | 目的 |
|---|---|---:|---|
| 首页/图片下载 | `/` | 是 | 完成核心下载任务 |
| 视频下载说明 | `/pinterest-video-downloader/` | 条件式 | 仅在功能和内容确实独立时发布 |
| 使用说明 | `/how-to-use/` | 是 | 解释受支持链接与步骤 |
| 隐私政策 | `/privacy/` | 是 | 说明日志和数据保留 |
| 使用条款 | `/terms/` | 是 | 明确允许用途和责任边界 |
| 版权/投诉 | `/copyright/` | 是 | 提供权利人投诉入口 |
| 联系方式 | `/contact/` | 是 | 用户反馈与纠错 |
| 解析结果状态 | 页面内动态区域 | 否 | 展示用户特定结果 |
| API 路由 | `/api/*` | 否 | 产品接口 |

### 8.2 首页信息架构

```text
Header：Logo / How to use / Privacy
Hero：H1 / 简短价值说明 / URL 输入框 / 主按钮
Result：预览 / 媒体信息 / 下载版本 / 错误状态
Supported URLs：支持与不支持的链接类型
How it works：3 个真实步骤
Limitations：可用性、权限、格式与质量边界
Privacy：不长期保存文件和 URL 的说明
FAQ：基于真实用户问题，不堆砌关键词
Footer：Terms / Privacy / Copyright / Contact / Non-affiliation
```

### 8.3 UI/UX 设计规范（线路 A：下载工具）

本节只适用于 SavePinner，不得将 PinCraft 的 AI SaaS 视觉、Vibe 选择器或付费升级交互混入本项目。实现依据为《线路 A / 线路 B —— UI/UX 参考全解析》中线路 A 和两线共用部分。

#### 8.3.1 核心交互原则

- 用户心态是“用完即走、零耐心”，核心指标是速度感和零摩擦。
- 核心路径保持“粘贴链接 → 解析 → 下载”，不增加注册、弹窗或无关按钮。
- 首屏必须直接展示完整工具，用户不向下滚动、不跳到其他页面即可发起解析。
- 工具、落地页内容和结果展示合为一个精品工具页面：工具在首屏，说明内容在工具下方，结果在当前页面动态展开。
- 每增加一个弹窗、无关动作或不必要页面跳转，都视为核心流程回退。

#### 8.3.2 首页布局

```text
Sticky Header
  ├─ SVG Logo
  ├─ Home
  ├─ Blog
  ├─ About Us
  └─ Browser Extension（扩展实际发布后才显示）

Hero（bg-red-50）
  ├─ H1：text-3xl，md:text-5xl，bold
  ├─ 一句话说明：gray-500
  └─ 下载工具条
      ├─ Pinterest URL 输入框：gray-50
      ├─ Paste 按钮
      └─ Download 主按钮

Trust Badges
  ├─ SSL Secure
  ├─ No Login Required
  ├─ Community Trusted（有真实依据后）
  └─ Malware Free（有持续安全验证后）

How to Use（bg-gray-50）
  ├─ Mobile：3 步
  └─ Desktop：3 步

FAQ + Related Links
Footer：Privacy / Terms / Copyright / Contact / Non-affiliation
```

- Header 使用白色半透明背景、`bg-white/85` 和 `backdrop-blur` 的 sticky 形式。
- Hero 使用浅红背景，不使用深色大面积背景阻碍工具识别。
- 工具输入区保持单一主任务；Paste 和 Download 是首屏唯二核心操作。
- MVP 未发布浏览器扩展时，不得展示无法使用的扩展安装入口。

#### 8.3.3 视觉系统

| 项目 | SavePinner 规范 |
|---|---|
| 主色 | Pinterest 红 `#e60023` |
| 页面底色 | 白色；Hero 使用 `bg-red-50` 浅红区 |
| 次级信息 | 灰色系 |
| CTA | 大号红色 Download 按钮，直接醒目 |
| 字体 | Inter 或 System Font Stack |
| 圆角 | 12 px，保持全站一致 |
| 阴影 | 少用，以加载速度和清晰层级优先 |
| Logo | SVG，不使用低清位图或外部图床 Logo |

使用 Pinterest 红是视觉呼应，不得配合文案、Logo 或徽章暗示 SavePinner 属于 Pinterest 官方生态；非关联声明继续生效。

#### 8.3.4 Paste、加载与结果交互

- 输入框提供独立 Paste 按钮，优先减少移动端从 Pinterest 复制链接后返回网站的操作成本。
- Paste 使用清晰的剪贴板图标和文本/可访问名称，Download 使用下载图标和文本，不能只靠图标表达操作。
- Download 点击后立即进入可见加载状态，并采用 10% → 35% → 65% → 100% 的分段进度反馈。
- 分段进度用于说明系统正在解析，不得在结果尚未就绪时提前完成或伪装真实下载百分比。
- 结果区在原页面展开，展示预览、媒体信息和可用下载版本，不弹出新页面。
- 页面完成一次任务后，保留“处理另一个链接”的快捷入口。

#### 8.3.5 Trust Badges 的真实性门槛

文章提出的四枚横排徽章用于消除下载站用户对登录、病毒和安全性的顾虑，但每一枚都必须可验证：

| 徽章 | 展示条件 |
|---|---|
| SSL Secure | 生产环境全站 HTTPS 且证书正常 |
| No Login Required | 核心下载流程确实无需登录 |
| Community Trusted | 已有可核验的真实用户、评价或社区依据；上线初期无依据则不显示 |
| Malware Free | 下载链路与文件安全机制已经验证并持续监控；未达到时不作绝对承诺 |

徽章在桌面端横排，在移动端 `flex-wrap`；不能以徽章替代 Privacy、Terms 和版权说明。

#### 8.3.6 How to Use

- Mobile 和 Desktop 必须分成两套各 3 步的说明，不混为同一流程。
- 每步使用“图标 + Step N + 标题 + 简短说明”。
- 移动端步骤图使用手机框 mockup 承载真实界面，不直接散放未经处理的截图。
- 桌面端说明展示桌面浏览器中的真实操作路径。
- 图片必须服务于步骤理解，不能阻挡首屏工具或显著增加首屏加载时间。

#### 8.3.7 动态内容

- 用户自己的解析结果是页面最主要的动态内容。
- 可以在结果区或其下方展示“近期真实下载量”，但数据必须来自真实统计并注明统计口径；不得硬编码 `3,000+ downloads today` 等示例数字。
- 如果无法提供真实、可核验且不侵犯隐私的数据，则不展示下载量。

#### 8.3.8 性能与广告

- 养站和产品验证阶段保持零广告，不接入会拖慢首屏或制造弹窗的广告脚本。
- Logo 使用 SVG。
- 页面图片使用 Next.js Image 或等价优化能力，提供正确尺寸、WebP/现代格式、懒加载；首屏关键图片按实际需要设置高优先级，非首屏图片不得抢占带宽。
- 下载工具以功能可靠和响应清晰优先，不为“界面炫酷”引入重型动效或依赖。

#### 8.3.9 后续能力的 UI 参考

- **多语言**：KlickPin 的多语言子目录和完整 hreflang 是增长参考，但不改变 MVP 英语首发决策；每个语言仍需独立验证并提供真实本地化内容。
- **浏览器扩展**：Chrome、Edge、Firefox 扩展是后续第二曲线；只有扩展实际可安装时，导航栏才出现入口。
- **Board Download**：若阶段 4 以后立项，交互采用“搜索用户名 → 选择 Board → 一键下载整板”的顺序；深色主题和紫色渐变只作为 Pinterest Board Saver 设计稿参考，不属于当前 SavePinner 主站视觉规范。

### 8.4 文案要求

- 不使用“支持所有 Pinterest 内容”等绝对表达。
- 未验证原始分辨率时，不使用“Original”。
- 不提供“去水印”功能时，不使用“No Watermark”作为承诺。
- 明确提示用户仅下载自己拥有或有权使用的内容。
- 页脚和条款页明确 SavePinner 与 Pinterest 无关联或背书关系。

---

## 9. SEO 与索引要求

### 9.1 技术要求

- 首页核心正文、标题和导航在初始 HTML 中可见。
- 每个索引页具有唯一的 title、description、H1 和 canonical。
- sitemap 只包含返回 200、允许索引、自 canonical 的页面。
- `/api/*`、结果参数、错误状态和临时令牌不得进入索引。
- 主要内链使用可抓取的 `<a href>`。
- 页面返回真实 HTTP 状态，避免错误页一律返回 200。
- 生产环境检查 robots、canonical、hreflang 和结构化数据。

### 9.2 页面策略

- 同一用户任务的关键词变体合并到一个页面。
- 只有功能、结果和主体内容均明显不同，才创建独立下载器页面。
- 不为关键词拼写、设备、语言或格式批量生成近重复页面。
- 多语言仅在英文版达到产品指标、且目标语言需求已验证后启动。
- Blog 内容必须提供独立信息增益，例如兼容性表、错误排查、质量对比或权限说明。

### 9.3 搜索指标

以下为观察指标，不是上线验收承诺：

- 有效索引页数量。
- GSC 展示、点击、CTR 和平均排名。
- 有查询数据的页面比例。
- 品牌词与非品牌词流量比例。
- 页面重复、抓取异常和软 404 数量。

---

## 10. 非功能性需求

### 10.1 性能

| 指标 | MVP 目标 |
|---|---:|
| LCP | 移动端实验室环境 ≤ 2.5 秒 |
| CLS | ≤ 0.1 |
| INP | ≤ 200 毫秒 |
| 解析接口 p50 | ≤ 1.5 秒 |
| 解析接口 p95 | ≤ 4 秒 |
| 页面首屏 JS | 在实现后设定预算并持续监控 |

### 10.2 可用性

- 表单、按钮和结果卡支持键盘操作。
- 交互控件触摸区域不小于 44 × 44 CSS px。
- 正文与 UI 文字符合 WCAG AA 对比度要求。
- 加载、空状态、失败和成功状态均有可见反馈。
- 移动端不出现横向滚动或按钮遮挡。

### 10.3 可靠性

- 单个解析规则失效不得导致整个站点无法访问。
- 外部请求统一设置连接和总超时。
- 解析器输出经过 schema 校验。
- 上游结构变化通过监控告警，不静默返回错误结果。
- 支持快速关闭视频或特定解析器的功能开关。

### 10.4 隐私

- 默认不保存用户下载文件。
- 应用日志不记录完整查询参数、完整远程媒体 URL 或下载令牌。
- IP 仅用于限流和安全防护，并设置最短必要保留期。
- Privacy 页面说明处理目的、保留时间和联系渠道。

---

## 11. 安全与防滥用

### 11.1 SSRF 防护

- 只接受 HTTPS。
- 入口域名使用精确允许列表，不使用包含匹配。
- 重定向后重新校验协议、主机、端口和路径。
- 拒绝 localhost、私有 IP、链路本地地址、保留地址和非标准端口。
- 防范 DNS rebinding，连接前后验证解析地址。
- 不把上游响应中的任意重定向直接交给客户端。

### 11.2 下载代理防护

- 客户端不能指定任意代理 URL。
- 下载目标由服务端解析并封装进签名令牌。
- 按 IP、令牌和全局并发数限流。
- 设置单文件大小、总传输时长和每日带宽上限。
- 校验 MIME、文件签名和响应状态。
- 安全生成文件名，防止响应头注入。

### 11.3 初始限额

| 项目 | 初始值 | 说明 |
|---|---:|---|
| 解析请求 | 10 次/IP/分钟 | 上线后按数据调整 |
| 并发解析 | 3 次/IP | 防止批量滥用 |
| 重定向 | 最多 3 次 | 每次重新校验 |
| HTML 响应上限 | 2 MB | 超限终止 |
| 媒体文件上限 | 图片 25 MB；视频 250 MB | 需结合部署平台限制复核 |
| 解析超时 | 8 秒 | 包含上游请求 |
| 下载令牌有效期 | 5 分钟 | 不包含原始 URL |

---

## 12. 技术方案

### 12.1 推荐技术栈

| 层级 | 方案 | 备注 |
|---|---|---|
| Web 框架 | 当前稳定版 Next.js App Router | 开发前确认版本与运行时兼容性 |
| 样式 | Tailwind CSS | 控制依赖数量 |
| 部署 | Vercel 或 Cloudflare | 必须以实测成功率、成本和条款为准 |
| 校验 | Zod 或等价 schema 工具 | 校验请求和解析结果 |
| 限流 | 平台级限流或外部 KV | 不依赖单实例内存 |
| 监控 | 结构化日志 + 错误追踪 | 记录错误码和耗时，不记录敏感 URL |

### 12.2 逻辑模块

```text
app/
├── page.tsx
├── how-to-use/page.tsx
├── privacy/page.tsx
├── terms/page.tsx
├── copyright/page.tsx
├── contact/page.tsx
├── api/
│   ├── resolve/route.ts
│   └── download/[token]/route.ts
├── robots.ts
└── sitemap.ts

components/
├── DownloaderForm.tsx
├── ResultCard.tsx
├── ErrorState.tsx
├── HowItWorks.tsx
└── Limitations.tsx

lib/
├── pinterest/
│   ├── validate-url.ts
│   ├── resolve-short-url.ts
│   ├── fetch-pin.ts
│   ├── parse-media.ts
│   └── verify-variant.ts
├── download-token.ts
├── rate-limit.ts
├── errors.ts
└── logger.ts
```

### 12.3 解析策略

解析器属于高风险、易变化模块，必须与页面 UI 解耦。

候选来源按可信度排序：

1. Pinterest 官方授权 API，仅用于接口允许且用户已授权的内容。
2. 官方公开嵌入信息，用于预览或元数据，不假设提供原始文件。
3. 公开页面中可验证的元数据。
4. 页面结构解析，仅作为实验性 fallback，并受平台条款和稳定性评估约束。

禁止仅通过把 CDN 路径替换为 `/originals/` 就宣称原图可用；所有变体都必须实际验证。

---

## 13. 合规要求

### 13.1 上线前必须完成

- 审查 Pinterest 服务条款、开发者政策和社区准则。
- 明确产品是否只处理用户拥有/获授权内容，或处理任意公开内容。
- 准备 Privacy、Terms、Copyright/DMCA、Contact 和非关联声明。
- 明确权利人投诉、下架和重复侵权处理流程。
- 不在品牌、域名或页面视觉中暗示 Pinterest 官方关系。
- 审查目标部署平台对代理下载、带宽和版权投诉的政策。

### 13.2 用户提示

核心输入框附近显示：

> 请仅下载你拥有或已获得许可使用的内容。SavePinner 不托管 Pinterest 内容，也不是 Pinterest 的官方产品。

免责声明不能替代合法的数据访问方式，也不能消除版权责任。

---

## 14. 数据埋点与监控

### 14.1 产品事件

| 事件 | 触发条件 | 关键属性 |
|---|---|---|
| `resolve_started` | 用户提交有效格式 URL | device、locale |
| `resolve_succeeded` | 返回至少一个有效变体 | media_type、variant_count、duration_bucket |
| `resolve_failed` | 解析失败 | error_code、duration_bucket |
| `download_clicked` | 用户点击下载 | media_type、variant_label |
| `download_started` | 服务端开始流式响应 | media_type、size_bucket |
| `download_failed` | 下载中止 | error_code、size_bucket |

禁止把完整 Pin URL、完整媒体 URL 或下载令牌发送给分析平台。

### 14.2 核心指标

| 指标 | 公式 | MVP 门槛 |
|---|---|---:|
| URL 有效率 | 合法 URL 请求 / 所有提交 | 仅观察 |
| 解析成功率 | 成功解析 / 合法 URL 请求 | 测试集 ≥ 90% |
| 下载启动率 | 下载启动 / 解析成功 | ≥ 50% |
| 系统错误率 | 5xx / 所有解析请求 | < 2% |
| p95 解析耗时 | 解析请求第 95 百分位 | ≤ 4 秒 |
| 单次成功解析成本 | 基础设施成本 / 成功解析数 | 上线后建立基线 |

---

## 15. 测试与验收

### 15.1 测试集

上线前建立至少 100 条仅供测试的合法样本：

- 40 条图片 Pin。
- 20 条视频 Pin。
- 10 条 GIF/动态内容。
- 10 条 `pin.it` 短链。
- 10 条无效、已删除或不可访问链接。
- 10 条恶意或越界输入。

样本需记录测试日期、预期媒体类型、所有权/使用授权和实际结果。

### 15.2 功能验收

- 所有 P0 用户流程在移动端和桌面端通过。
- 首屏直接包含输入框、Paste 和 Download；核心任务不发生页面跳转。
- Clipboard API 可用时 Paste 按钮可一键填入链接；权限拒绝或不支持时有手动粘贴提示。
- 解析加载按 10% → 35% → 65% → 100% 提供分段反馈，结果未就绪前不显示 100%。
- How to Use 同时包含 Mobile 和 Desktop 两套各 3 步说明，移动端步骤使用手机框 mockup。
- Trust Badges 只展示满足真实性门槛的项目，不出现虚构社区信任或未经验证的安全承诺。
- 全站使用 SVG Logo、统一 12 px 圆角、红白灰视觉体系，养站期没有广告脚本干扰核心流程。
- 解析成功时至少一个变体经过服务端验证。
- 失败状态均返回标准错误码和可操作提示。
- 下载接口无法代理任意第三方 URL。
- 重定向、私有 IP、超大文件和高频请求测试通过。
- 结果页、API、参数页和临时令牌未进入 sitemap。

### 15.3 上线验收

- 生产首页返回 200，canonical 正确。
- robots.txt 和 sitemap 可访问且仅包含预期页面。
- Privacy、Terms、Copyright 和 Contact 页面完整。
- 生产环境无密钥泄露，日志不包含完整 URL 或令牌。
- 错误监控、限流和功能开关可用。
- 完成至少一次故障回滚演练。

---

## 16. 变现要求

### 16.1 MVP 阶段

- 不接广告，先验证产品成功率、流量质量与政策边界。
- 不把 AdSense 审核通过写入产品验收标准。
- 不购买以传递排名权重为目的的外链。

### 16.2 广告接入门槛

同时满足以下条件后才评估 AdSense：

- 核心功能稳定，系统错误率和投诉率可控。
- 站点具有原创说明、限制、隐私与帮助内容，不是薄工具壳。
- 已完成版权和平台政策审查。
- 广告位与输入框、预览、下载按钮保持明显距离。
- 广告容器预留尺寸，不造成 CLS。
- 目标地区的 CMP/同意管理要求已落实。

### 16.3 收入模型

采用情景模型，不使用固定收入承诺：

```text
有效页面浏览
= 月访问量 × 每次访问页数 ×（1 - 广告拦截率）

广告收入
= 有效页面浏览 ÷ 1000 × 页面 RPM
```

所有假设必须注明地区、设备、时间范围和数据来源。

---

## 17. 交付阶段与上线门槛

### 阶段 0：技术与合规验证（2-3 天）

**交付物**：

- 公开数据来源说明。
- 100 条测试集中的首轮解析结果。
- Pinterest 和部署平台政策核对记录。
- 图片、GIF、视频分别的成功率与失败原因。

**继续门槛**：

- 明确可接受的数据访问路径。
- 图片样本成功率达到 90%，不存在系统性错误结果。
- SSRF、开放代理和文件大小风险有明确方案。

不满足时停止开发或转向“用户授权的自有内容导出”。

### 阶段 1：核心 MVP（5-7 个工作日）

**范围**：

- 首页、URL 校验、图片解析、结果卡和受控下载。
- 标准错误模型、限流、日志和安全校验。
- Privacy、Terms、Copyright、Contact 页面。

视频与 GIF 只有在阶段 0 验证通过后才纳入。

### 阶段 2：小流量发布（1-2 周）

- 发布英语版。
- 接入 GSC/Bing 和产品分析。
- 观察解析成功率、用户错误、带宽和投诉。
- 不购买付费外链，不批量生成页面。

### 阶段 3：增长验证（第 3-8 周）

- 根据真实查询和失败数据决定是否发布独立视频页。
- 创建 2-3 篇具有独立信息增益的帮助内容。
- 通过真实使用数据验证广告和基础设施经济性。

### 阶段 4：扩展（第 2-3 个月）

只有同时满足以下条件才启动多语言或 Board 功能：

- 英语版核心任务稳定。
- 目标语言或功能存在独立需求证据。
- 页面具备独立价值，不是翻译或关键词替换薄页。
- 带宽、支持和合规成本仍可接受。

满足上述门槛后，UI/UX 扩展方向按以下顺序评估：

1. 对已验证语言建立独立子目录和 hreflang，不一次性机械上线 80+ 语言。
2. 评估 Chrome、Edge、Firefox 浏览器扩展；扩展上线后再将安装入口加入导航。
3. 若 Board Download 合规与技术验证通过，采用“搜索用户名 → 选择 Board → 一键下载整板”的交互范本。

---

## 18. 风险清单

| 风险 | 概率 | 影响 | 优先级 | 缓解措施 |
|---|---:|---:|---:|---|
| 未经授权的自动化访问违反平台条款 | 高 | 高 | P0 | 阶段 0 完成合规审查；优先官方授权路径 |
| 内容版权或商标投诉 | 中 | 高 | P0 | 限定用途、非关联声明、投诉/下架流程；避免误导品牌关系 |
| 页面结构变化导致解析失效 | 高 | 高 | P0 | 解耦解析器、测试集、告警、功能开关和快速回滚 |
| 下载接口成为开放代理或 SSRF 入口 | 中 | 高 | P0 | 允许列表、签名令牌、IP 校验、限流和大小限制 |
| 上游封锁数据中心 IP | 中 | 高 | P1 | 不把更换 IP 当长期方案；验证授权接口或调整产品范围 |
| Serverless 带宽/执行成本失控 | 中 | 高 | P1 | 流量限额、成本告警、分级文件上限和经济模型 |
| AdSense 不批准或限制广告 | 中 | 中 | P1 | 不以广告审批作为 MVP 成功前提；增加原创价值和合规页面 |
| 关键词竞争或需求被高估 | 中 | 高 | P1 | 当前 SERP、搜索量、CTR 和实际 GSC 数据验证 |
| 近重复页面导致索引质量下降 | 中 | 中 | P1 | 按任务合并页面；建立 URL 准入标准 |
| “原图/无水印”承诺不准确 | 高 | 中 | P1 | 只展示验证后的实际属性，使用“最高可用质量” |

---

## 19. Go / No-Go 标准

### 19.1 Go

满足以下条件才继续投入增长：

- 数据访问方式的合规与稳定性风险可接受。
- 100 条测试集总体解析成功率达到 90%，图片达到 95%。
- 下载代理通过安全测试，无法转发非允许主机。
- 用户能够清楚区分可用质量、失败原因与版权边界。
- 小流量运行成本在保守广告或其他收入模型下可承受。
- 至少一个页面通过真实查询数据证明存在可点击需求。

### 19.2 No-Go 或转向

出现任一条件，应停止匿名公开内容下载方向：

- 核心能力必须依赖未经允许的抓取或持续绕过风控。
- 页面结构变化导致成功率长期低于 80%。
- 投诉、封锁或带宽成本超过可接受范围。
- 产品差异只剩“更漂亮、更快、免费”。
- 搜索需求不足以支撑保守收入情景。

推荐转向：用户通过 OAuth 授权后，备份或导出自己拥有的 Pins 和 Boards。

---

## 20. 待确认问题

1. 产品是否只处理用户拥有/已获授权的内容，还是处理任意公开 Pin？
2. 是否愿意在数据路径不合规或不稳定时转向 OAuth 自有内容导出？
3. MVP 是否只做图片，视频和 GIF 在验证通过后再加入？
4. 生产部署选择 Vercel 还是 Cloudflare，二者的代理下载条款与成本是否已经核验？
5. 单文件大小、每日带宽和免费用户额度是多少？
6. 是否需要保存下载历史？本 PRD 默认不保存。
7. 用户投诉与版权下架由谁处理，目标响应时间是多少？
8. 域名和品牌是否已完成商标与历史检查？
9. 首发语言是否确认只做英语？
10. MVP 的正式负责人、预算和发布日期是什么？

---

## 21. 决策记录

| 日期 | 决策 | 原因 | 状态 |
|---|---|---|---|
| 2026-07-20 | 将市场数据降级为待验证假设 | 原始材料缺少可复核查询日期和来源 | 已确认 |
| 2026-07-20 | 下载接口改为签名令牌，不接受任意 URL | 防止开放代理和 SSRF | 已确认 |
| 2026-07-20 | 多语言、Board、扩展不进入 MVP | 降低范围和近重复页面风险 | 已确认 |
| 2026-07-20 | 视频/GIF 进入条件式范围 | 需先验证技术成功率与平台边界 | 已确认 |
| 2026-07-20 | AdSense 延后至产品稳定后 | 降低审核、误点和合规风险 | 已确认 |
| 2026-07-20 | 纳入线路 A UI/UX 规范 | 明确粘贴下载、红白视觉、移动/桌面教程、真实徽章与后续扩展边界 | 已确认 |

---

## 22. 参考资料

### 22.1 官方资料

- [Pinterest Developer Platform](https://developers.pinterest.com/docs/)
- [Pinterest Developer Guidelines](https://policy.pinterest.com/en/developer-guidelines)
- [Pinterest Terms of Service](https://policy.pinterest.com/en/terms-of-service)
- [Pinterest Community Guidelines](https://policy.pinterest.com/en/community-guidelines)
- [Pinterest：下载图片](https://help.pinterest.com/en/article/download-an-image)
- [Google Search Spam Policies](https://developers.google.com/search/docs/essentials/spam-policies)
- [Google JavaScript SEO 基础](https://developers.google.com/search/docs/crawling-indexing/javascript/javascript-seo-basics)
- [Google AdSense 广告位置政策](https://support.google.com/adsense/answer/1346295)

### 22.2 原始材料中的参考项目

- pinflik.com
- pinpea.com
- klickpin.com
- pintsave.com
- SaveInsta（下载工具交互参考）
- [Pinterest Board Saver 设计稿](https://dribbble.com/shots/24183060)（仅用于未来 Board Download 交互参考）

竞品排名、流量、DR、域龄、技术栈和可用性均需按当前日期重新核验，不能直接沿用历史快照。

### 22.3 UI/UX 需求来源

- 用户提供的《线路 A / 线路 B —— UI/UX 参考全解析》：本 PRD 只采用其中“线路 A：下载工具”和“两线共用 UI 原则”，未采用线路 B 的 AI SaaS 视觉与付费交互。

---

## 23. MVP 完成定义（Definition of Done）

MVP 只有在以下条件全部满足时才视为完成：

- P0 功能需求全部通过验收。
- 技术与合规验证有书面结论。
- 测试集、成功率和主要失败原因已记录。
- 安全测试确认不存在明显 SSRF 或开放代理漏洞。
- Privacy、Terms、Copyright、Contact 和非关联声明已上线。
- 生产环境 robots、sitemap、canonical 和 HTTP 状态正确。
- 监控、限流、成本告警和功能开关可用。
- 至少一名非开发人员在移动端独立完成核心任务。
- 已形成 Go / No-Go 决策，不以“页面已部署”代替产品完成。
